export interface isShowSideNavStyle {
  isShowSideNav: boolean;
}

export interface Button {
  color: string;
  bgColor: string;
}

export interface InputStyled {
  isDark?: boolean;
}

export interface InitialsProps {
  fontSize: string;
  sizeBox: string;
  background: string;
  color: string;
}