export interface SignupAdminData {
  email: string;
  firstName: string;
  lastName: string;
  password: string;
  phoneNumber: string;
}

export interface LoginUserData {
  email: string;
  password: string;
}